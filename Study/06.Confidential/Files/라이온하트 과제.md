---
Index:
  - "[[6. ConfidentialIndex]]"
tags:
  - Study
  - Confidential
---
   
## 1. 과제
---

```
#pragma once

//======================================================================================================
// <문제1>
//
// 각각의 스킬을 사용할 확률이 다음과 같다.
// Skill* pSkill1(15), pSkill2(25), pSkill3(10)
// 각각의 확률 값은 상대적인 값이므로 실제 확률은 30%(15/50), 50%(25/50), 20%(10/50)가 된다.

// pSkill1, pSkill2, pSkill3와 상대 확률(15, 25, 10)의 입력값으로 랜덤하게 하나의 스킬(pSkill*)을 선택하는 함수를 작성하라. 

// 입력값(pSkill1, 15)은 꼭 3개가 아니라 1개일수도 있고 수십개 일수도 있다.
// 또한 입력값의 타입이 특정 타입의 클래스(Skill)가 아니더라도 결과를 얻어낼 수 있어야 한다. 템플릿 사용 필요.

// 다음과 같은 형태의 함수 2개가 필요하고 해당 기능을 가진 클래스는 스킬에 종속적이어서는 안 된다.
// void AddRandom(int Prob, const T& Data); //확률과 데이터 입력
// bool GetRandom(T& Data) const;			//확률에 따라 데이터 1개를 리턴하고 값이 없으면 false 리턴
//======================================================================================================

//======================================================================================================
// <문제2>
//
// 게임에서 사용하는 아이템 드랍 시스템을 설계하려 한다. 

// <문제1>과 동일한 형태로 확률이 적용되며 AddRandom 함수와 GetRandom 함수를 사용해도 된다. 30%(15/50), 50%(25/50), 20%(10/50), ...

// DropID와 ClassType(Warrior, Sorceress, Rogue)을 입력하면 입력된 ClassType에 맞는 ItemID를 랜덤하게 리턴한다.

// struct ItemInfo와 DropInfo를 정의하고 테스트를 위한 최소한의 데이터를 임의로 입력하라.
// 주어진 조건을 만족하도록 GetRandomItem 함수를 구현하라.
// 구현한 내용이 실제 게임에서 어떤 식으로 사용될 지에 대해 설명하시오.
//======================================================================================================

//===================================================================================================================
// <문제3 - 클라>
//
// Player가 Monster를 향해 발사체를 날렸고 이 발사체는 적을 따라 움직이는 유도미사일이다.
// 각 객체는 위치정보 m_PosX, m_PosY, m_PosZ와 속도값 m_Velocity를 가지고 있고 몬스터는 랜덤하게 움직이면서 도망간다고 가정한다.
// 이때 발사체의 유도미사일 기능을 중심으로 클래스를 구현하라.
// 예외적인 상황이 없을지에 대해서 충분히 검토하고 본인이 가장 합리적이라고 생각하는 정책으로 구현하면 된다.
// Player, Monster, Missile 클래스를 만든후 유도미사일 기능을 위한 최소한의 기능만 구현한다.
// 각 객체가 소멸하는 상황에 대한 예외처리를 고민할 것!
//===================================================================================================================
```
